# Third-party notices

## Application runtime

- **Tauri** — MIT OR Apache-2.0 · <https://tauri.app/> · <https://github.com/tauri-apps/tauri>
- **Rust crates** — the full dependency list is in `src-tauri/Cargo.lock`; each crate ships its own
  license text. The notable ones are `reqwest` (MIT OR Apache-2.0), `serde` / `serde_json`
  (MIT OR Apache-2.0) and the `tauri-plugin-*` family (MIT OR Apache-2.0).

Windows builds use the system **WebView2** runtime, which is a Microsoft component and is not
redistributed with this app.

## Build-time tools

These are declared under `devDependencies` and are **not** bundled into the built app:

- **sharp** — Apache License 2.0 · rasterises the brand SVG into the app icons
  (`scripts/generate-brand-icons.mjs`). Distributed with libvips and its applicable third-party
  notices.
- **@tauri-apps/cli** — MIT OR Apache-2.0 · builds and bundles the app.

## Artwork

- The gray cat / fox / rabbit / Bulu animations were generated from project-provided references and
  ship with this repository; the robot is procedurally generated by `scripts/generate-sprites.mjs`.
- If you replace them with third-party art, verify the license yourself (CC0 / MIT / OGA-BY 3.0
  recommended, and credit the source in the README).
